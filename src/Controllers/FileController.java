package Controllers;

public class FileController {

	//method that loops through entire usersList.txt and for each user, loads info into the "Users" directory
	
	//method that goes through all files starting from "data/Users" and adds them to the appropriate arrays
	
}
